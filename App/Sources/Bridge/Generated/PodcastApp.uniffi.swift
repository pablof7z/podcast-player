// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(nmp_app_podcastFFI)
import nmp_app_podcastFFI
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_nmp_app_podcast_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_nmp_app_podcast_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureNmpAppPodcastInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
fileprivate final class UniffiHandleMap<T>: @unchecked Sendable {
    // All mutation happens with this lock held, which is why we implement @unchecked Sendable.
    private let lock = NSLock()
    private var map: [UInt64: T] = [:]
    private var currentHandle: UInt64 = 1

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            let handle = currentHandle
            currentHandle += 1
            map[handle] = obj
            return handle
        }
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.
// Magic number for the Rust proxy to call using the same mechanism as every other method,
// to free the callback once it's dropped by Rust.
private let IDX_CALLBACK_FREE: Int32 = 0
// Callback return codes
private let UNIFFI_CALLBACK_SUCCESS: Int32 = 0
private let UNIFFI_CALLBACK_ERROR: Int32 = 1
private let UNIFFI_CALLBACK_UNEXPECTED_ERROR: Int32 = 2

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt32: FfiConverterPrimitive {
    typealias FfiType = UInt32
    typealias SwiftType = UInt32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterBool : FfiConverter {
    typealias FfiType = Int8
    typealias SwiftType = Bool

    public static func lift(_ value: Int8) throws -> Bool {
        return value != 0
    }

    public static func lower(_ value: Bool) -> Int8 {
        return value ? 1 : 0
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bool {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Bool, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        return String(bytes: bytes, encoding: String.Encoding.utf8)!
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        return String(bytes: try readBytes(&buf, count: Int(len)), encoding: String.Encoding.utf8)!
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterData: FfiConverterRustBuffer {
    typealias SwiftType = Data

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Data {
        let len: Int32 = try readInt(&buf)
        return Data(try readBytes(&buf, count: Int(len)))
    }

    public static func write(_ value: Data, into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        writeBytes(&buf, value)
    }
}




/**
 * The app-owned UniFFI object. Owns the single `NmpApp` instance and the
 * app-domain `PodcastHandle`.
 */
public protocol PodcastAppProtocol: AnyObject, Sendable {

    func cancelBunkerHandshake()

    func classifyInputIntent(requestJson: String)  -> String

    func configure(visibleLimit: UInt32, emitHz: UInt32)

    func consumeAllBuiltinProjections()

    /**
     * `profile_json`/`relays_json` keep the same wire shape the legacy
     * C-ABI symbol used (`HashMap<String, String>` / `Vec<(String,
     * String)>` JSON) rather than a UniFFI record — this is a 1:1 behavior
     * port, not a redesign; the JSON shape is owned by the Swift call site
     * today and changing it is out of scope for wave 1.
     */
    func createNewAccount(profileJson: String, relaysJson: String, mls: Bool, makeActive: Bool)

    func decodeNip21Uri(input: String)  -> String

    func decodeUpdateFrame(frame: Data)  -> String?

    func deliverExternalSignerResponse(responseJson: String)

    func dispatchAction(envelope: Data)  -> PodcastDispatchOutcome

    func dispatchCapabilityJson(requestJson: String)  -> String

    func dispatchInputIntent(requestJson: String, sessionId: String?)  -> String

    func dispatchPodcastAction(namespace: String, actionJson: String)  -> String?

    func isAlive()  -> Bool

    func lifecycleBackground()

    func lifecycleForeground()

    func nostrconnectUri(callbackScheme: String?)  -> String?

    func podcastBridgeCall(endpoint: String, requestJson: String?)  -> String?

    /**
     * Transitional escape hatch for the still-C-ABI app-domain tail.
     * This returns the `PodcastHandle` pointer owned by this `PodcastApp`;
     * Swift must not free it.
     */
    func podcastHandle()  -> UInt64

    func podcastSnapshot()  -> String?

    func podcastSnapshotRev()  -> UInt64

    func releaseRef(namespace: PodcastRefNamespace, key: String, consumerId: String)

    func removeAccount(identityId: String)

    func reset()

    func resolveRef(namespace: PodcastRefNamespace, key: String, consumerId: String, shape: PodcastRefShape, liveness: PodcastRefLiveness)

    func setAgentAskSink(sink: PodcastAgentAskSink?)

    func setCapabilityCallback(sink: PodcastCapabilitySink?)

    func setPodcastDataDir(path: String)

    func setStoragePath(path: String?)

    func setUpdateSink(sink: PodcastUpdateSink?)

    func shutdown()

    func signEventForReturn(accountPubkeyHex: String, unsignedJson: String)  -> String

    func signerBrokerInit()

    func signinBunker(uri: String, makeActive: Bool)

    func signinNip55(signerPackage: String?)

    func signinNsec(secret: String, makeActive: Bool)

    func start(visibleLimit: UInt32, emitHz: UInt32)

    func stop()

}
/**
 * The app-owned UniFFI object. Owns the single `NmpApp` instance and the
 * app-domain `PodcastHandle`.
 */
open class PodcastApp: PodcastAppProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_nmp_app_podcast_fn_clone_podcastapp(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_nmp_app_podcast_fn_constructor_podcastapp_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_nmp_app_podcast_fn_free_podcastapp(pointer, $0) }
    }




open func cancelBunkerHandshake()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_cancel_bunker_handshake(self.uniffiClonePointer(),$0
    )
}
}

open func classifyInputIntent(requestJson: String) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_classify_input_intent(self.uniffiClonePointer(),
        FfiConverterString.lower(requestJson),$0
    )
})
}

open func configure(visibleLimit: UInt32, emitHz: UInt32)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_configure(self.uniffiClonePointer(),
        FfiConverterUInt32.lower(visibleLimit),
        FfiConverterUInt32.lower(emitHz),$0
    )
}
}

open func consumeAllBuiltinProjections()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_consume_all_builtin_projections(self.uniffiClonePointer(),$0
    )
}
}

    /**
     * `profile_json`/`relays_json` keep the same wire shape the legacy
     * C-ABI symbol used (`HashMap<String, String>` / `Vec<(String,
     * String)>` JSON) rather than a UniFFI record — this is a 1:1 behavior
     * port, not a redesign; the JSON shape is owned by the Swift call site
     * today and changing it is out of scope for wave 1.
     */
open func createNewAccount(profileJson: String, relaysJson: String, mls: Bool, makeActive: Bool)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_create_new_account(self.uniffiClonePointer(),
        FfiConverterString.lower(profileJson),
        FfiConverterString.lower(relaysJson),
        FfiConverterBool.lower(mls),
        FfiConverterBool.lower(makeActive),$0
    )
}
}

open func decodeNip21Uri(input: String) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_decode_nip21_uri(self.uniffiClonePointer(),
        FfiConverterString.lower(input),$0
    )
})
}

open func decodeUpdateFrame(frame: Data) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_decode_update_frame(self.uniffiClonePointer(),
        FfiConverterData.lower(frame),$0
    )
})
}

open func deliverExternalSignerResponse(responseJson: String)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_deliver_external_signer_response(self.uniffiClonePointer(),
        FfiConverterString.lower(responseJson),$0
    )
}
}

open func dispatchAction(envelope: Data) -> PodcastDispatchOutcome  {
    return try!  FfiConverterTypePodcastDispatchOutcome_lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_dispatch_action(self.uniffiClonePointer(),
        FfiConverterData.lower(envelope),$0
    )
})
}

open func dispatchCapabilityJson(requestJson: String) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_dispatch_capability_json(self.uniffiClonePointer(),
        FfiConverterString.lower(requestJson),$0
    )
})
}

open func dispatchInputIntent(requestJson: String, sessionId: String?) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_dispatch_input_intent(self.uniffiClonePointer(),
        FfiConverterString.lower(requestJson),
        FfiConverterOptionString.lower(sessionId),$0
    )
})
}

open func dispatchPodcastAction(namespace: String, actionJson: String) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_dispatch_podcast_action(self.uniffiClonePointer(),
        FfiConverterString.lower(namespace),
        FfiConverterString.lower(actionJson),$0
    )
})
}

open func isAlive() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_is_alive(self.uniffiClonePointer(),$0
    )
})
}

open func lifecycleBackground()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_lifecycle_background(self.uniffiClonePointer(),$0
    )
}
}

open func lifecycleForeground()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_lifecycle_foreground(self.uniffiClonePointer(),$0
    )
}
}

open func nostrconnectUri(callbackScheme: String?) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_nostrconnect_uri(self.uniffiClonePointer(),
        FfiConverterOptionString.lower(callbackScheme),$0
    )
})
}

open func podcastBridgeCall(endpoint: String, requestJson: String?) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_podcast_bridge_call(self.uniffiClonePointer(),
        FfiConverterString.lower(endpoint),
        FfiConverterOptionString.lower(requestJson),$0
    )
})
}

    /**
     * Transitional escape hatch for the still-C-ABI app-domain tail.
     * This returns the `PodcastHandle` pointer owned by this `PodcastApp`;
     * Swift must not free it.
     */
open func podcastHandle() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_podcast_handle(self.uniffiClonePointer(),$0
    )
})
}

open func podcastSnapshot() -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_podcast_snapshot(self.uniffiClonePointer(),$0
    )
})
}

open func podcastSnapshotRev() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_podcast_snapshot_rev(self.uniffiClonePointer(),$0
    )
})
}

open func releaseRef(namespace: PodcastRefNamespace, key: String, consumerId: String)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_release_ref(self.uniffiClonePointer(),
        FfiConverterTypePodcastRefNamespace_lower(namespace),
        FfiConverterString.lower(key),
        FfiConverterString.lower(consumerId),$0
    )
}
}

open func removeAccount(identityId: String)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_remove_account(self.uniffiClonePointer(),
        FfiConverterString.lower(identityId),$0
    )
}
}

open func reset()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_reset(self.uniffiClonePointer(),$0
    )
}
}

open func resolveRef(namespace: PodcastRefNamespace, key: String, consumerId: String, shape: PodcastRefShape, liveness: PodcastRefLiveness)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_resolve_ref(self.uniffiClonePointer(),
        FfiConverterTypePodcastRefNamespace_lower(namespace),
        FfiConverterString.lower(key),
        FfiConverterString.lower(consumerId),
        FfiConverterTypePodcastRefShape_lower(shape),
        FfiConverterTypePodcastRefLiveness_lower(liveness),$0
    )
}
}

open func setAgentAskSink(sink: PodcastAgentAskSink?)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_set_agent_ask_sink(self.uniffiClonePointer(),
        FfiConverterOptionCallbackInterfacePodcastAgentAskSink.lower(sink),$0
    )
}
}

open func setCapabilityCallback(sink: PodcastCapabilitySink?)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_set_capability_callback(self.uniffiClonePointer(),
        FfiConverterOptionCallbackInterfacePodcastCapabilitySink.lower(sink),$0
    )
}
}

open func setPodcastDataDir(path: String)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_set_podcast_data_dir(self.uniffiClonePointer(),
        FfiConverterString.lower(path),$0
    )
}
}

open func setStoragePath(path: String?)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_set_storage_path(self.uniffiClonePointer(),
        FfiConverterOptionString.lower(path),$0
    )
}
}

open func setUpdateSink(sink: PodcastUpdateSink?)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_set_update_sink(self.uniffiClonePointer(),
        FfiConverterOptionCallbackInterfacePodcastUpdateSink.lower(sink),$0
    )
}
}

open func shutdown()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_shutdown(self.uniffiClonePointer(),$0
    )
}
}

open func signEventForReturn(accountPubkeyHex: String, unsignedJson: String) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_sign_event_for_return(self.uniffiClonePointer(),
        FfiConverterString.lower(accountPubkeyHex),
        FfiConverterString.lower(unsignedJson),$0
    )
})
}

open func signerBrokerInit()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_signer_broker_init(self.uniffiClonePointer(),$0
    )
}
}

open func signinBunker(uri: String, makeActive: Bool)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_signin_bunker(self.uniffiClonePointer(),
        FfiConverterString.lower(uri),
        FfiConverterBool.lower(makeActive),$0
    )
}
}

open func signinNip55(signerPackage: String?)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_signin_nip55(self.uniffiClonePointer(),
        FfiConverterOptionString.lower(signerPackage),$0
    )
}
}

open func signinNsec(secret: String, makeActive: Bool)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_signin_nsec(self.uniffiClonePointer(),
        FfiConverterString.lower(secret),
        FfiConverterBool.lower(makeActive),$0
    )
}
}

open func start(visibleLimit: UInt32, emitHz: UInt32)  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_start(self.uniffiClonePointer(),
        FfiConverterUInt32.lower(visibleLimit),
        FfiConverterUInt32.lower(emitHz),$0
    )
}
}

open func stop()  {try! rustCall() {
    uniffi_nmp_app_podcast_fn_method_podcastapp_stop(self.uniffiClonePointer(),$0
    )
}
}


}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastApp: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PodcastApp

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PodcastApp {
        return PodcastApp(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PodcastApp) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastApp {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PodcastApp, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastApp_lift(_ pointer: UnsafeMutableRawPointer) throws -> PodcastApp {
    return try FfiConverterTypePodcastApp.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastApp_lower(_ value: PodcastApp) -> UnsafeMutableRawPointer {
    return FfiConverterTypePodcastApp.lower(value)
}




public struct PodcastDispatchOutcome {
    public var correlationId: String?
    public var error: String?
    public var code: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(correlationId: String?, error: String?, code: String?) {
        self.correlationId = correlationId
        self.error = error
        self.code = code
    }
}

#if compiler(>=6)
extension PodcastDispatchOutcome: Sendable {}
#endif


extension PodcastDispatchOutcome: Equatable, Hashable {
    public static func ==(lhs: PodcastDispatchOutcome, rhs: PodcastDispatchOutcome) -> Bool {
        if lhs.correlationId != rhs.correlationId {
            return false
        }
        if lhs.error != rhs.error {
            return false
        }
        if lhs.code != rhs.code {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(correlationId)
        hasher.combine(error)
        hasher.combine(code)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastDispatchOutcome: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastDispatchOutcome {
        return
            try PodcastDispatchOutcome(
                correlationId: FfiConverterOptionString.read(from: &buf),
                error: FfiConverterOptionString.read(from: &buf),
                code: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: PodcastDispatchOutcome, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.correlationId, into: &buf)
        FfiConverterOptionString.write(value.error, into: &buf)
        FfiConverterOptionString.write(value.code, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastDispatchOutcome_lift(_ buf: RustBuffer) throws -> PodcastDispatchOutcome {
    return try FfiConverterTypePodcastDispatchOutcome.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastDispatchOutcome_lower(_ value: PodcastDispatchOutcome) -> RustBuffer {
    return FfiConverterTypePodcastDispatchOutcome.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum PodcastEventShape {

    case embed
    case raw
}


#if compiler(>=6)
extension PodcastEventShape: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastEventShape: FfiConverterRustBuffer {
    typealias SwiftType = PodcastEventShape

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastEventShape {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .embed

        case 2: return .raw

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PodcastEventShape, into buf: inout [UInt8]) {
        switch value {


        case .embed:
            writeInt(&buf, Int32(1))


        case .raw:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastEventShape_lift(_ buf: RustBuffer) throws -> PodcastEventShape {
    return try FfiConverterTypePodcastEventShape.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastEventShape_lower(_ value: PodcastEventShape) -> RustBuffer {
    return FfiConverterTypePodcastEventShape.lower(value)
}


extension PodcastEventShape: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum PodcastProfileShape {

    case ref
    case card
}


#if compiler(>=6)
extension PodcastProfileShape: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastProfileShape: FfiConverterRustBuffer {
    typealias SwiftType = PodcastProfileShape

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastProfileShape {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .ref

        case 2: return .card

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PodcastProfileShape, into buf: inout [UInt8]) {
        switch value {


        case .ref:
            writeInt(&buf, Int32(1))


        case .card:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastProfileShape_lift(_ buf: RustBuffer) throws -> PodcastProfileShape {
    return try FfiConverterTypePodcastProfileShape.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastProfileShape_lower(_ value: PodcastProfileShape) -> RustBuffer {
    return FfiConverterTypePodcastProfileShape.lower(value)
}


extension PodcastProfileShape: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum PodcastRefLiveness {

    case cacheOk
    case live
}


#if compiler(>=6)
extension PodcastRefLiveness: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastRefLiveness: FfiConverterRustBuffer {
    typealias SwiftType = PodcastRefLiveness

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastRefLiveness {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .cacheOk

        case 2: return .live

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PodcastRefLiveness, into buf: inout [UInt8]) {
        switch value {


        case .cacheOk:
            writeInt(&buf, Int32(1))


        case .live:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefLiveness_lift(_ buf: RustBuffer) throws -> PodcastRefLiveness {
    return try FfiConverterTypePodcastRefLiveness.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefLiveness_lower(_ value: PodcastRefLiveness) -> RustBuffer {
    return FfiConverterTypePodcastRefLiveness.lower(value)
}


extension PodcastRefLiveness: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum PodcastRefNamespace {

    case profile
    case event
}


#if compiler(>=6)
extension PodcastRefNamespace: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastRefNamespace: FfiConverterRustBuffer {
    typealias SwiftType = PodcastRefNamespace

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastRefNamespace {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .profile

        case 2: return .event

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PodcastRefNamespace, into buf: inout [UInt8]) {
        switch value {


        case .profile:
            writeInt(&buf, Int32(1))


        case .event:
            writeInt(&buf, Int32(2))

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefNamespace_lift(_ buf: RustBuffer) throws -> PodcastRefNamespace {
    return try FfiConverterTypePodcastRefNamespace.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefNamespace_lower(_ value: PodcastRefNamespace) -> RustBuffer {
    return FfiConverterTypePodcastRefNamespace.lower(value)
}


extension PodcastRefNamespace: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum PodcastRefShape {

    case profile(shape: PodcastProfileShape
    )
    case event(shape: PodcastEventShape
    )
}


#if compiler(>=6)
extension PodcastRefShape: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePodcastRefShape: FfiConverterRustBuffer {
    typealias SwiftType = PodcastRefShape

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PodcastRefShape {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        case 1: return .profile(shape: try FfiConverterTypePodcastProfileShape.read(from: &buf)
        )

        case 2: return .event(shape: try FfiConverterTypePodcastEventShape.read(from: &buf)
        )

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PodcastRefShape, into buf: inout [UInt8]) {
        switch value {


        case let .profile(shape):
            writeInt(&buf, Int32(1))
            FfiConverterTypePodcastProfileShape.write(shape, into: &buf)


        case let .event(shape):
            writeInt(&buf, Int32(2))
            FfiConverterTypePodcastEventShape.write(shape, into: &buf)

        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefShape_lift(_ buf: RustBuffer) throws -> PodcastRefShape {
    return try FfiConverterTypePodcastRefShape.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePodcastRefShape_lower(_ value: PodcastRefShape) -> RustBuffer {
    return FfiConverterTypePodcastRefShape.lower(value)
}


extension PodcastRefShape: Equatable, Hashable {}









public protocol PodcastAgentAskSink: AnyObject, Sendable {

    func onAgentAskEvent(eventJson: String)

}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfacePodcastAgentAskSink {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // This creates 1-element array, since this seems to be the only way to construct a const
    // pointer that we can pass to the Rust code.
    static let vtable: [UniffiVTableCallbackInterfacePodcastAgentAskSink] = [UniffiVTableCallbackInterfacePodcastAgentAskSink(
        onAgentAskEvent: { (
            uniffiHandle: UInt64,
            eventJson: RustBuffer,
            uniffiOutReturn: UnsafeMutableRawPointer,
            uniffiCallStatus: UnsafeMutablePointer<RustCallStatus>
        ) in
            let makeCall = {
                () throws -> () in
                guard let uniffiObj = try? FfiConverterCallbackInterfacePodcastAgentAskSink.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return uniffiObj.onAgentAskEvent(
                     eventJson: try FfiConverterString.lift(eventJson)
                )
            }


            let writeReturn = { () }
            uniffiTraitInterfaceCall(
                callStatus: uniffiCallStatus,
                makeCall: makeCall,
                writeReturn: writeReturn
            )
        },
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            let result = try? FfiConverterCallbackInterfacePodcastAgentAskSink.handleMap.remove(handle: uniffiHandle)
            if result == nil {
                print("Uniffi callback interface PodcastAgentAskSink: handle missing in uniffiFree")
            }
        }
    )]
}

private func uniffiCallbackInitPodcastAgentAskSink() {
    uniffi_nmp_app_podcast_fn_init_callback_vtable_podcastagentasksink(UniffiCallbackInterfacePodcastAgentAskSink.vtable)
}

// FfiConverter protocol for callback interfaces
#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterCallbackInterfacePodcastAgentAskSink {
    fileprivate static let handleMap = UniffiHandleMap<PodcastAgentAskSink>()
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
extension FfiConverterCallbackInterfacePodcastAgentAskSink : FfiConverter {
    typealias SwiftType = PodcastAgentAskSink
    typealias FfiType = UInt64

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ handle: UInt64) throws -> SwiftType {
        try handleMap.get(handle: handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ v: SwiftType) -> UInt64 {
        return handleMap.insert(obj: v)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func write(_ v: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(v))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastAgentAskSink_lift(_ handle: UInt64) throws -> PodcastAgentAskSink {
    return try FfiConverterCallbackInterfacePodcastAgentAskSink.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastAgentAskSink_lower(_ v: PodcastAgentAskSink) -> UInt64 {
    return FfiConverterCallbackInterfacePodcastAgentAskSink.lower(v)
}




public protocol PodcastCapabilitySink: AnyObject, Sendable {

    func onCapabilityRequest(requestJson: String)  -> String

}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfacePodcastCapabilitySink {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // This creates 1-element array, since this seems to be the only way to construct a const
    // pointer that we can pass to the Rust code.
    static let vtable: [UniffiVTableCallbackInterfacePodcastCapabilitySink] = [UniffiVTableCallbackInterfacePodcastCapabilitySink(
        onCapabilityRequest: { (
            uniffiHandle: UInt64,
            requestJson: RustBuffer,
            uniffiOutReturn: UnsafeMutablePointer<RustBuffer>,
            uniffiCallStatus: UnsafeMutablePointer<RustCallStatus>
        ) in
            let makeCall = {
                () throws -> String in
                guard let uniffiObj = try? FfiConverterCallbackInterfacePodcastCapabilitySink.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return uniffiObj.onCapabilityRequest(
                     requestJson: try FfiConverterString.lift(requestJson)
                )
            }


            let writeReturn = { uniffiOutReturn.pointee = FfiConverterString.lower($0) }
            uniffiTraitInterfaceCall(
                callStatus: uniffiCallStatus,
                makeCall: makeCall,
                writeReturn: writeReturn
            )
        },
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            let result = try? FfiConverterCallbackInterfacePodcastCapabilitySink.handleMap.remove(handle: uniffiHandle)
            if result == nil {
                print("Uniffi callback interface PodcastCapabilitySink: handle missing in uniffiFree")
            }
        }
    )]
}

private func uniffiCallbackInitPodcastCapabilitySink() {
    uniffi_nmp_app_podcast_fn_init_callback_vtable_podcastcapabilitysink(UniffiCallbackInterfacePodcastCapabilitySink.vtable)
}

// FfiConverter protocol for callback interfaces
#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterCallbackInterfacePodcastCapabilitySink {
    fileprivate static let handleMap = UniffiHandleMap<PodcastCapabilitySink>()
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
extension FfiConverterCallbackInterfacePodcastCapabilitySink : FfiConverter {
    typealias SwiftType = PodcastCapabilitySink
    typealias FfiType = UInt64

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ handle: UInt64) throws -> SwiftType {
        try handleMap.get(handle: handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ v: SwiftType) -> UInt64 {
        return handleMap.insert(obj: v)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func write(_ v: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(v))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastCapabilitySink_lift(_ handle: UInt64) throws -> PodcastCapabilitySink {
    return try FfiConverterCallbackInterfacePodcastCapabilitySink.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastCapabilitySink_lower(_ v: PodcastCapabilitySink) -> UInt64 {
    return FfiConverterCallbackInterfacePodcastCapabilitySink.lower(v)
}




public protocol PodcastUpdateSink: AnyObject, Sendable {

    func onUpdate(frame: Data)

}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfacePodcastUpdateSink {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // This creates 1-element array, since this seems to be the only way to construct a const
    // pointer that we can pass to the Rust code.
    static let vtable: [UniffiVTableCallbackInterfacePodcastUpdateSink] = [UniffiVTableCallbackInterfacePodcastUpdateSink(
        onUpdate: { (
            uniffiHandle: UInt64,
            frame: RustBuffer,
            uniffiOutReturn: UnsafeMutableRawPointer,
            uniffiCallStatus: UnsafeMutablePointer<RustCallStatus>
        ) in
            let makeCall = {
                () throws -> () in
                guard let uniffiObj = try? FfiConverterCallbackInterfacePodcastUpdateSink.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return uniffiObj.onUpdate(
                     frame: try FfiConverterData.lift(frame)
                )
            }


            let writeReturn = { () }
            uniffiTraitInterfaceCall(
                callStatus: uniffiCallStatus,
                makeCall: makeCall,
                writeReturn: writeReturn
            )
        },
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            let result = try? FfiConverterCallbackInterfacePodcastUpdateSink.handleMap.remove(handle: uniffiHandle)
            if result == nil {
                print("Uniffi callback interface PodcastUpdateSink: handle missing in uniffiFree")
            }
        }
    )]
}

private func uniffiCallbackInitPodcastUpdateSink() {
    uniffi_nmp_app_podcast_fn_init_callback_vtable_podcastupdatesink(UniffiCallbackInterfacePodcastUpdateSink.vtable)
}

// FfiConverter protocol for callback interfaces
#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterCallbackInterfacePodcastUpdateSink {
    fileprivate static let handleMap = UniffiHandleMap<PodcastUpdateSink>()
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
extension FfiConverterCallbackInterfacePodcastUpdateSink : FfiConverter {
    typealias SwiftType = PodcastUpdateSink
    typealias FfiType = UInt64

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ handle: UInt64) throws -> SwiftType {
        try handleMap.get(handle: handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        let handle: UInt64 = try readInt(&buf)
        return try lift(handle)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ v: SwiftType) -> UInt64 {
        return handleMap.insert(obj: v)
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func write(_ v: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(v))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastUpdateSink_lift(_ handle: UInt64) throws -> PodcastUpdateSink {
    return try FfiConverterCallbackInterfacePodcastUpdateSink.lift(handle)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterCallbackInterfacePodcastUpdateSink_lower(_ v: PodcastUpdateSink) -> UInt64 {
    return FfiConverterCallbackInterfacePodcastUpdateSink.lower(v)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionCallbackInterfacePodcastAgentAskSink: FfiConverterRustBuffer {
    typealias SwiftType = PodcastAgentAskSink?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterCallbackInterfacePodcastAgentAskSink.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterCallbackInterfacePodcastAgentAskSink.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionCallbackInterfacePodcastCapabilitySink: FfiConverterRustBuffer {
    typealias SwiftType = PodcastCapabilitySink?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterCallbackInterfacePodcastCapabilitySink.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterCallbackInterfacePodcastCapabilitySink.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionCallbackInterfacePodcastUpdateSink: FfiConverterRustBuffer {
    typealias SwiftType = PodcastUpdateSink?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterCallbackInterfacePodcastUpdateSink.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterCallbackInterfacePodcastUpdateSink.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}
public func podcastBridgeGlobalCall(endpoint: String, requestJson: String) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_nmp_app_podcast_fn_func_podcast_bridge_global_call(
        FfiConverterString.lower(endpoint),
        FfiConverterString.lower(requestJson),$0
    )
})
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private let initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 29
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_nmp_app_podcast_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_func_podcast_bridge_global_call() != 52926) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_cancel_bunker_handshake() != 641) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_classify_input_intent() != 22761) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_configure() != 15231) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_consume_all_builtin_projections() != 37081) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_create_new_account() != 5059) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_decode_nip21_uri() != 30743) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_decode_update_frame() != 33239) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_deliver_external_signer_response() != 56059) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_dispatch_action() != 26141) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_dispatch_capability_json() != 21258) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_dispatch_input_intent() != 24629) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_dispatch_podcast_action() != 5545) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_is_alive() != 46134) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_lifecycle_background() != 59774) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_lifecycle_foreground() != 44456) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_nostrconnect_uri() != 64833) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_podcast_bridge_call() != 54916) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_podcast_handle() != 9309) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_podcast_snapshot() != 121) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_podcast_snapshot_rev() != 37686) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_release_ref() != 65391) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_remove_account() != 29909) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_reset() != 65280) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_resolve_ref() != 38717) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_set_agent_ask_sink() != 1815) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_set_capability_callback() != 42066) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_set_podcast_data_dir() != 50106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_set_storage_path() != 54228) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_set_update_sink() != 58508) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_shutdown() != 31636) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_sign_event_for_return() != 58530) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_signer_broker_init() != 9113) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_signin_bunker() != 16445) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_signin_nip55() != 17171) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_signin_nsec() != 29789) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_start() != 5787) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastapp_stop() != 61874) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_constructor_podcastapp_new() != 33241) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastagentasksink_on_agent_ask_event() != 32244) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastcapabilitysink_on_capability_request() != 55021) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_nmp_app_podcast_checksum_method_podcastupdatesink_on_update() != 19397) {
        return InitializationResult.apiChecksumMismatch
    }

    uniffiCallbackInitPodcastAgentAskSink()
    uniffiCallbackInitPodcastCapabilitySink()
    uniffiCallbackInitPodcastUpdateSink()
    return InitializationResult.ok
}()

// Make the ensure init function public so that other modules which have external type references to
// our types can call it.
public func uniffiEnsureNmpAppPodcastInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all