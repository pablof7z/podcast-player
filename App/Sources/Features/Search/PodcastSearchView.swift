import SwiftUI

// TODO: Podcast-corpus search. Discover new shows + semantic search over
// transcripts/wiki. Distinct from the template's `UniversalSearchView`, which
// searches local items/notes/memories.

struct PodcastSearchView: View {
    var body: some View {
        Text("Search")
            .padding()
    }
}
