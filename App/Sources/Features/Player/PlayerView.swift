import SwiftUI

// TODO: Full-screen player. Reached by expanding the persistent mini-bar.
// Hosts cover art, scrubber, transcript-following, agent commands, share-clip.

struct PlayerView: View {
    var body: some View {
        Text("Player")
            .padding()
    }
}
