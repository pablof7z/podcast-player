import SwiftUI

// TODO: Voice conversational mode. Push-to-talk and ambient/always-on modes.
// Visualizes mic level, agent thinking state, TTS playback, and supports
// barge-in (user can interrupt the agent mid-utterance).

struct VoiceView: View {
    var body: some View {
        Text("Voice")
            .padding()
    }
}
