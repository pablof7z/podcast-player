import SwiftUI

// TODO: Single-episode detail. Show notes, transcript, related wiki entries,
// "ask about this episode" entry point into the agent.

struct EpisodeDetailView: View {
    var body: some View {
        Text("Episode Detail")
            .padding()
    }
}
