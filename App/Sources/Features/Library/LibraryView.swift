import SwiftUI

// TODO: Library tab. Subscriptions grid + recent episodes feed. Editorial
// typography, generous whitespace, parallax artwork on scroll.

struct LibraryView: View {
    var body: some View {
        Text("Library")
            .padding()
    }
}
