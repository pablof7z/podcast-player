import SwiftUI

// TODO: Wiki tab. Browse + search LLM-generated podcast/episode/topic pages.
// Surface related episodes inline, deep link to player at relevant timestamps.

struct WikiView: View {
    var body: some View {
        Text("Wiki")
            .padding()
    }
}
