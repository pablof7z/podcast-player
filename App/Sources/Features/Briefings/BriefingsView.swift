import SwiftUI

// TODO: Briefings inbox. Lists past + draft TLDR audio briefings. Generate-now
// affordance. Each briefing is an interruptible synthesized "episode."

struct BriefingsView: View {
    var body: some View {
        Text("Briefings")
            .padding()
    }
}
