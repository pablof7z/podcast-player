import SwiftUI

// TODO: "Today" landing tab. Editorial home: a curated catch-up feed, the
// week's TLDR briefing, in-progress episodes, and a single ambient agent
// prompt. The screen should feel calm — alive surfaces appear on demand.

struct TodayView: View {
    var body: some View {
        Text("Today")
            .padding()
    }
}
